# Example environment variables
# Copy this to .env.local and adjust as needed

# Path to trading state file (relative to project root)
STATE_FILE_PATH=../data/trading_state/safe_multi_symbol_state.json

# Optional: API endpoint if using remote state access
# TRADING_API_URL=http://localhost:8000/api/state
